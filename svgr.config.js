// svgr.config.js
// SVGR will convert them into React components that are styled dynamically (based on CSS) using currentColor.
module.exports = {
    svgProps: {
      width: '100%',
      height: '100%',
    //   fill: 'currentColor',
    },
    expandProps: 'start',
  };
  